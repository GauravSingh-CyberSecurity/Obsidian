
```
sudo apt update 
sudo apt upgrade 
sudo apt dist-upgrade
sudo apt-get dist-upgrade 
sudo apt-get dist-update


sudo do-release-upgrade

sudo do-release-upgrade -h    (i.e help)

sudo do-release-upgrade -d  
(i.e latest version upgrade)
```
